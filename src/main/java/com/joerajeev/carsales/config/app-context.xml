<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jee="http://www.springframework.org/schema/jee" xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd 
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">

	<context:annotation-config></context:annotation-config>

	<!-- DAO CONTEXT -->

	<!-- Scans the given package for components and makes them available in 
		the container for wireing etc -->
	<context:component-scan base-package="com.joerajeev.carsales.dao"></context:component-scan>

	<!-- Looks up jndi resource by given name and creates a bean called dataSource -->
	<jee:jndi-lookup jndi-name="jdbc/carsalesdb" id="dataSource"></jee:jndi-lookup>


	<!-- SECURITY CONTEXT -->

	<security:authentication-manager>
		<security:authentication-provider>
			<security:jdbc-user-service
				data-source-ref="dataSource"
				users-by-username-query="select * from (select email as username, password, enabled from users) users where username = ?" />
			<security:password-encoder ref="passwordEncoder"></security:password-encoder>
		</security:authentication-provider>
	</security:authentication-manager>

	<security:http auto-config="true">
		<security:intercept-url pattern="/"
			access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<security:intercept-url pattern="/static/**"
			access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<security:intercept-url pattern="/buy"
			access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<security:intercept-url pattern="/login"
			access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<security:intercept-url pattern="/logged-out"
			access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<security:intercept-url pattern="/sign-up"
			access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<security:intercept-url pattern="/docreateuser"
			access="IS_AUTHENTICATED_ANONYMOUSLY" />
		<security:intercept-url pattern="/sell" access="ROLE_USER" />
		<security:intercept-url pattern="/docreatecar"
			access="ROLE_USER" />
		<security:intercept-url pattern="/docreatead"
			access="ROLE_USER" />
		<security:intercept-url pattern="/**" access="ROLE_ADMIN" />
		<security:form-login login-page="/login"
			authentication-failure-url="/login?error" />
		<security:logout logout-success-url="/?msg=logged-out" />
	</security:http>
	<bean id="webexpressionHandler"
		class="org.springframework.security.web.access.expression.DefaultWebSecurityExpressionHandler">
	</bean>
	<bean id="passwordEncoder"
		class="org.springframework.security.crypto.password.StandardPasswordEncoder">
	</bean>

	<!-- SERVICE CONTEXT -->
	
	<!-- Scans the given service package for Service components -->
	<context:component-scan
		base-package="com.joerajeev.carsales.service">
	</context:component-scan>
</beans>
